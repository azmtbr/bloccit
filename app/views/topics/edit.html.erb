<h1>Edit Topic</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for topics:</p>
    <ul>
      <li>Make sure the topic is appropriate.</li>
      <li>Never insult animals.</li>
      <li>Smile while you type.</li>
    </ul>
  </div>
  <div class="col-me-8">
    <%= render partial: 'form', locals: { topic: @topic } %>
  </div>
  <div class="form-group">
    <%= render partial: 'labels/form', locals: { f: f, labels: @topic.labels } %>
  </div>
  <div class="form-group">
       <%= render partial: 'ratings/form', locals: { f: f, ratings: @topic.ratings } %>
  </div>
</div>
