$('<%= "#post-#{@post.id}" %>').html("<%= escape_javascript(render partial: 'votes/voter', locals: { post: @post }) %>");
$('#voting_scorecard').html("<%= escape_javascript(render partial: 'votes/scorecard', locals: { post: @post }) %>");
