<%= puts "Here are the number of votes with this post." %>
<%= puts @post.points %>
$( '<%= "#post-#{@post.id}.voting" %>').html("<%= escape_javascript(render partial: 'votes/voter', locals: { post: @post }) %>");
